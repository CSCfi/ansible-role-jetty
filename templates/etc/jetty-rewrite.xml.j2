<?xml version="1.0"?>
<!DOCTYPE Configure PUBLIC "-//Jetty//Configure//EN" "http://www.eclipse.org/jetty/configure_9_3.dtd">
<!--
WARNING:
this file is auto-generated by Ansible CSCfi.jetty role
DO NOT EDIT
all local changes will be overwritten!
-->

<Configure id="Server" class="org.eclipse.jetty.server.Server">

  <!-- =========================================================== -->
  <!-- configure rewrite handler                                   -->
  <!-- =========================================================== -->
  <Call name="insertHandler">
    <Arg>
      <New class="org.eclipse.jetty.rewrite.handler.RewriteHandler">
        <Set name="rewriteRequestURI"><Property name="jetty.rewrite.rewriteRequestURI" deprecated="rewrite.rewriteRequestURI" default="true"/></Set>
        <Set name="rewritePathInfo"><Property name="jetty.rewrite.rewritePathInfo" deprecated="rewrite.rewritePathInfo" default="false"/></Set>
        <Set name="originalPathAttribute"><Property name="jetty.rewrite.originalPathAttribute" deprecated="rewrite.originalPathAttribute" default="requestedPath"/></Set>

        <!-- Set DispatcherTypes  -->
        <Set name="dispatcherTypes">
        <Array type="javax.servlet.DispatcherType">
            <Item><Call class="javax.servlet.DispatcherType" name="valueOf"><Arg>REQUEST</Arg></Call></Item>
            <Item><Call class="javax.servlet.DispatcherType" name="valueOf"><Arg>ASYNC</Arg></Call></Item>
        </Array>
        </Set>

        <Get id="Rewrite" name="ruleContainer"/>

        <Set name="rules">
            <Array type="org.eclipse.jetty.rewrite.handler.Rule">

            <Item>
                <New id="header" class="org.eclipse.jetty.rewrite.handler.HeaderPatternRule">
                <Set name="pattern">*</Set>
                <Set name="name">Cache-Control</Set>
{% if jetty_headers.cache_control is defined %}
                <Set name="value">{{ jetty_headers.cache_control }}</Set>
{% else %}
                <Set name="value">no-store, no-cache, must-revalidate, proxy-revalidate</Set>
{% endif %}
                </New>
            </Item>
            <Item>
                <New id="header" class="org.eclipse.jetty.rewrite.handler.HeaderPatternRule">
                <Set name="pattern">*</Set>
                <Set name="name">Expect-CT</Set>
{% if jetty_headers.expect_ct is defined %}
                <Set name="value">{{ jetty_headers.expect_ct }}</Set>
{% else %}
                <Set name="value">enforce, max-age=86400</Set>
{% endif %}
                </New>
            </Item>
            <Item>
                <New id="header" class="org.eclipse.jetty.rewrite.handler.HeaderPatternRule">
                <Set name="pattern">*</Set>
                <Set name="name">Expires</Set>
{% if jetty_headers.expires is defined %}
                <Set name="value">{{ jetty_headers.expires }}</Set>
{% else %}
                <Set name="value">0</Set>
{% endif %}
                </New>
            </Item>
            <Item>
                <New id="header" class="org.eclipse.jetty.rewrite.handler.HeaderPatternRule">
                <Set name="pattern">*</Set>
                <Set name="name">Pragma</Set>
{% if jetty_headers.pragma is defined %}
                <Set name="value">{{ jetty_headers.pragma }}</Set>
{% else %}
                <Set name="value">no-cache</Set>
{% endif %}
                </New>
            </Item>
            <Item>
                <New id="header" class="org.eclipse.jetty.rewrite.handler.HeaderPatternRule">
                <Set name="pattern">*</Set>
                <Set name="name">Referrer-Policy</Set>
{% if jetty_headers.referrer_policy is defined %}
                <Set name="value">{{ jetty_headers.referrer_policy }}</Set>
{% else %}
                <Set name="value">same-origin</Set>
{% endif %}
                </New>
            </Item>
            <Item>
                <New id="header" class="org.eclipse.jetty.rewrite.handler.HeaderPatternRule">
                <Set name="pattern">*</Set>
                <Set name="name">Surrogate-Control</Set>
{% if jetty_headers.surrogate_control is defined %}
                <Set name="value">{{ jetty_headers.surrogate_control }}</Set>
{% else %}
                <Set name="value">no-store</Set>
{% endif %}
                </New>
            </Item>

            <Item>
                <New id="header" class="org.eclipse.jetty.rewrite.handler.HeaderPatternRule">
                <Set name="pattern">*</Set>
                <Set name="name">X-Content-Type-Options</Set>
{% if jetty_headers.x_content_type_options is defined %}
                <Set name="value">{{ jetty_headers.x_content_type_options }}</Set>
{% else %}
                <Set name="value">nosniff</Set>
{% endif %}
                </New>
            </Item>
            <Item>
                <New id="header" class="org.eclipse.jetty.rewrite.handler.HeaderPatternRule">
                <Set name="pattern">*</Set>
                <Set name="name">X-DNS-Prefetch-Control</Set>
{% if jetty_headers.x_dns_prefetch_control is defined %}
                <Set name="value">{{ jetty_headers.x_dns_prefetch_control }}</Set>
{% else %}
                <Set name="value">off</Set>
{% endif %}
                </New>
            </Item>
            <Item>
                <New id="header" class="org.eclipse.jetty.rewrite.handler.HeaderPatternRule">
                <Set name="pattern">*</Set>
                <Set name="name">X-Download-Options</Set>
{% if jetty_headers.x_download_options is defined %}
                <Set name="value">{{ jetty_headers.x_download_options }}</Set>
{% else %}
                <Set name="value">noopen</Set>
{% endif %}
                </New>
            </Item>
            <Item>
                <New id="header" class="org.eclipse.jetty.rewrite.handler.HeaderPatternRule">
                <Set name="pattern">*</Set>
                <Set name="name">X-Frame-Options</Set>
{% if jetty_headers.x_frame_options is defined %}
                <Set name="value">{{ jetty_headers.x_frame_options }}</Set>
{% else %}
                <Set name="value">SAMEORIGIN</Set>
{% endif %}
                </New>
            </Item>
            <Item>
                <New id="header" class="org.eclipse.jetty.rewrite.handler.HeaderPatternRule">
                <Set name="pattern">*</Set>
                <Set name="name">X-XSS-Protection</Set>
{% if jetty_headers.x_xss_protection is defined %}
                <Set name="value">{{ jetty_headers.x_xss_protection }}</Set>
{% else %}
                <Set name="value">1; mode=block</Set>
{% endif %}
                </New>
            </Item>

            </Array>
        </Set>
        <!-- see rewrite-compactpath.xml for example how to add a rule -->

      </New>
    </Arg>
  </Call>
</Configure>
